# 持续监控使用说明

## 一键启动

Windows 用户双击 `start_monitor.bat`，按提示输入可信 API、待测 API 和 key。间隔设置直接按回车即可，默认每轮结束后随机等待 20 到 40 秒。

Linux 或 macOS 用户运行：

```bash
sh start_monitor.sh
```

窗口必须保持开启。需要停止时按一次 `Ctrl+C`，程序会保留最新 JSON 报告。

## 如何判断

程序持续生成新的随机挑战，并滚动统计最近 20 个到达待测 API 的候选尝试。候选传输失败和发生过重试的轮次都保留在分母中：

- 完整加密状态至少命中 15 次；
- 删除 item ID 后至少命中 15 次；
- message-only 必须 0 次命中；
- 损坏密文必须 0 次命中；
- 请求明文泄漏必须为 0。

最初不足 20 个候选尝试时，状态为 `warming_up`。达到 20 个后，每产生一个新的候选尝试，就移出窗口中最旧的一个并更新判定。窗口内只要存在候选错误或重试，状态就是 `inconclusive_candidate_unstable`，不能通过。

候选接口返回 429、500、502、503、504 或发生连接错误时，程序会随机退避并有限重试；403 不重试。无论最终是否恢复，该轮都进入最近 20 个候选尝试的分母。这样不会把网络故障当成模型不兼容，也不会通过丢弃故障轮次来抬高命中率。

## 自动采用的随机化措施

- 每轮使用新的密码学随机十位挑战；
- 三种计算任务按候选尝试均衡轮换；
- 使用此前批量实验中稳定的规范种子提示；
- 四个条件统一使用实测稳定的规范召回提示，避免措辞差异污染结果；
- 四种候选条件随机调整发送顺序，并在请求之间随机等待 2–5 秒；
- 每轮结束后的等待时间在用户设置范围内密码学随机选择。

这些措施能降低按固定周期、固定提示词或固定请求顺序识别检测流量的效果，但不能做到隐身。中转站仍可能根据 `/v1/responses`、reasoning item 或整个协议形状识别探针，并把这类流量专门转发给 GPT-5.6。持续全部通过只能证明监控覆盖期间的同类请求稳定获得 GPT-5.6 兼容处理，不能保证所有普通业务请求使用相同路由。

## 费用和频率

每个候选尝试通常产生 2 个可信 API 请求和 4 个待测 API 请求。遇到瞬时传输错误时，单个候选条件默认最多重试 2 次，因此极端情况下待测请求数会增加。默认周期本身也包含请求耗时、2–5 秒条件间隔以及轮间 20–40 秒随机等待。

建议：

- 短时间排查动态路由：默认 20–40 秒，运行 30–60 分钟；
- 日常监控：设置 240–660 秒；
- 长期低成本监控：设置 600–1800 秒。

一次挑战执行本身也需要时间，因此实际频率低于简单的间隔除法。

## 报告字段

报告会在每轮完成后原子更新，主要查看：

- `total_candidate_attempts`：累计进入候选判定分母的挑战；
- `total_valid_trials`：四类候选响应均完整返回的挑战；
- `total_rejected_attempts`：可信端失败与候选传输失败的累计次数；
- `rolling_summary.verdict`：最近 20 个候选尝试的判定；
- `rolling_summary.candidate_error_rounds`：窗口内最终传输失败轮次；
- `rolling_summary.candidate_retry_rounds`：窗口内发生过重试的轮次；
- `rolling_summary.full_exact`：完整状态命中次数；
- `rolling_summary.without_ids_exact`：删除 ID 后命中次数；
- `rolling_summary.message_only_exact`：正文对照异常命中次数；
- `rolling_summary.corrupted_ciphertext_exact`：损坏密文异常命中次数。

报告不保存 API key、原始挑战、正确答案、候选原始答案或原始密文。



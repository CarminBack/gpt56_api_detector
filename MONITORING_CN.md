# v3.1.1 持续监控使用说明


## 一键启动

推荐 Windows 双击 `Windows一键启动.bat`，在界面中选择“持续监控”。左侧设置区支持鼠标滚轮滚动，运行日志统一按 UTF-8 显示。单次检测会显示本次样本，不复用持续监控的滚动窗口文案；持续监控右侧会实时显示当前结论、健康度、线路、强检测进度、五档 Juice 和高档字面量对照；“简明报告”页只列出当前模式需要关注的异常和统计。

Windows 双击 `start_monitor.bat`。先选择综合监控或仅 Juice 监控，再按提示输入接口和 key。仅 Juice 模式不会询问或连接可信端。时间设置直接回车时使用随机 150–210 秒。

图形界面的单次检测默认使用 8 路并发，以缩短等待；并发数可在 1–16 之间调整。综合单次检测以完整 COT 轮次为并发单位，每个 worker 独立完成可信状态生成、自证和四类待测挑战，不再只并发轮内变体，也不会形成嵌套并发。持续监控仍按设定的随机间隔逐轮执行。

Linux 或 macOS：

```bash
sh start_monitor.sh
```

窗口必须保持开启。按 `Ctrl+C` 停止，最新 JSON 报告会保留。


## 每轮会做什么

综合模式每轮：

1. 生成新的随机十位数字和本地正确答案；
2. 可信 API 生成加密状态并自证；
3. 自证成功后，待测 API 接收完整、去 ID、只有正文、损坏密文四种请求；
4. 独立执行本轮 Juice 请求；
5. 更新强检测窗口、型号指纹和综合结论；
6. 随机等待 150–210 秒后开始下一轮。

仅 Juice 模式跳过前三步，只请求待测端、更新 Juice 窗口和辅助结论。它不会产生 COT 能力结论。

每轮还会独立以 50% 概率增加一次 high 档字面量输出完整性对照。命中后再以 50% 概率选择 Luna 的 `48` 或 Terra 的 `32`，要求回复严格等于本轮指定值，并与 Juice 型号样本完全隔离。

可信端自证失败时，本轮不会执行四个强挑战，也不会进入强检测候选分母，但 Juice 请求仍会独立执行。


## juice 调度与提示变体

持续模式每轮固定请求高档。随后在“不加测、低、中、超高、最高”五种选择中均匀随机选一项，所以辅助档位出现概率为 4/5，且每轮最多只追加一个。

这保证主指纹持续刷新，又使辅助档位和每轮请求数不固定。每个提示都用恒等运算请求原始 Juice 数字，并加入一次性随机外壳和 nonce。

内部包加载 internal-identity-v1 私有模板池；公开包没有私有模块时自动回退 public-fallback。高档保留最近 20 条，其他四档分别保留最近 10 条。“高档出现过互斥型号”或“已知跨档冲突”是整个会话永久标记，不会随滚动窗口消失。

## 高档字面量输出完整性对照

- 单次检测固定额外执行 1 次；
- 持续监控每轮独立以 50% 概率执行 1 次；执行时 `48`/`32` 各占 50%；
- 最近 20 条单独统计，不进入 Sol/Terra/Luna 分类；
- 只有严格等于本轮指定的 `48` 或 `32` 才算精确通过，小数形式、其他数字、拒答或其他文字都算非预期；
- 任一成功的非预期响应触发严重警告，并在当前会话内粘住，重挂才清除；
- 传输错误单独计入线路结果，不冒充输出改写。

非预期输出只能证明结果没有按原指令到达，不能单独区分模型未遵循、系统提示干预或中转输出替换。

## 强检测怎样通过

最近 20 个候选尝试必须满足：

- 完整加密状态至少答对 15 次；
- 去掉状态编号后至少答对 15 次；
- 只有正文和损坏密文必须 0 次误命中；
- 明文泄漏必须为 0。

错误轮次仍保留在 20 次总数中，并且拿不到答对分，不能靠删除失败请求凑成功率。但错误或重试本身不再一票否决模型能力：实际命中仍达到 15 次时，强检测照常通过。

## 型号指纹怎样通过

- 至少 3 个有效高档数字；
- 三次主指纹一致；
- 至少一个其他档位支持；
- 与用户填写的模型标签一致；
- 整个会话没有出现互斥高档指纹。

出现 Sol 和 Luna 等互斥高档值时，控制台直接显示“检测到混用”，不采用多数票。

高档确定主型号后，其他档位若出现已知且明确排斥主型号的数字，也会直接标记混用。重合值不会误报，例如 Sol 的低档 `8` 同时也是 Luna/mini 的有效值，中档 `16` 也可能属于多个 5.6 变体。


## 屏幕上的中文是什么意思

- “还在检测”：还没有收集满 20 次；
- “历史检测通过，但当前监控未刷新”：旧窗口曾通过，但可信端异常，当前没有新候选；
- “综合检测通过”：强检测通过，具体型号也高置信一致，并且没有发现混用；
- “GPT-5.6 强检测已通过，具体型号待定”：模型能力证据足够，具体型号还需要样本；
- “检测到模型混用”：同一会话出现互相排斥的型号结果；
- “型号与申报不符”：强检测通过，但具体型号和填写的名字不同；
- “两种检测结果冲突”：强检测像 GPT-5.6，但型号数字更像较低型号；
- “检测异常”：本来不该答对的请求答对了，结果不能相信；
- “线路流畅 / 偶有波动 / 不稳定”：只说明接口连接质量，不改变已经达到门槛的模型能力结论。

每轮屏幕显示当前结论、进度、答对情况、具体型号、混用、五档各自通过数/数字数/滚动窗口占用、线路和本轮结果。高档主结论统一显示 `当前窗口条数/20`，不会再把 3 次高置信门槛写成进度分母。仅 Juice 模式会明确显示“模型能力：未检测”。

## 错误与重试

429、500、502、503、504 和连接中断默认最多重试 2 次，403 不重试。四个强检测请求之间随机等待 2–5 秒。

线路分三级：

- 没有最终失败、没有重试：“流畅”；
- 没有最终失败、但有请求重试后恢复：“偶有波动”；
- 至少一轮最终仍有请求失败：“不稳定”。

失败轮次仍占最近 20 次的名额并拿不到答对分，但不会额外触发“模型不通过”。型号数字请求的错误或拒答只算没有证据，不会冒充某个型号。

## 可信来源和证据新鲜度

报告的 updated_at 只代表进程最近写过报告，不能单独证明待测端最近收到过挑战。

monitor_health_summary 会另外保存：

- 可信端连续失败次数；
- 最后一次候选挑战时间；
- 候选证据距现在多久；
- 当前证据是否仍在刷新；
- 下次可信端重试前的等待时间。

连续失败 1–2 轮时显示“可信端有波动”。连续失败至少 3 轮，或候选证据超过自动计算的有效时间时，综合标题会改成“历史检测通过，但当前监控未刷新”。

可信端恢复并产生新候选后，这个状态自动回到“监控正常”。历史窗口和会话级混用记录不会因此清空。

连续失败后的重试间隔依次增加到 60 秒、120 秒和最高 240 秒，防止故障期间每几十秒持续请求可信端。
## 请求量

综合模式一个正常周期通常包含：

- 可信 API 2 次；
- 待测 API 强检测 4 次；
- 待测 API Juice 指纹 1–2 次。

仅 Juice 模式每轮只请求待测 API 1–2 次。

遇到瞬时错误时请求数会因有限重试增加。长期监控建议把轮间等待调到 240–660 秒或更长。


## 报告字段

主要字段：

- `total_candidate_attempts`：进入强检测分母的挑战数；
- `total_valid_trials`：四类强请求都完整返回的挑战数；
- `total_juice_probes`：浅层指纹请求数；
- `rolling_summary`：第一层最近 20 次结果；
- `network_summary`：独立的线路质量评价；
- `monitor_health_summary`：可信来源状态和候选证据新鲜度；
- `juice_summary`：型号、置信度、拒答、错误和会话混用标记；
- `juice_summary.effort_stats`：五档各自的通过数、数字样本数和窗口进度；
- `output_literal_control_summary`：最近 20 条对照计数、会话级粘性异常及严重警告状态；
- `output_literal_control_observations`：期望值、对应变体、请求、最多 128 字符的实际短输出、哈希和状态；
- `last_cycle_output_literal_control_enabled`：本轮 50% 随机选择是否命中；
- `last_cycle_output_literal_control_expected_text`：命中时本轮选择的 `48` 或 `32`；
- `combined_summary`：给普通用户看的综合结论；
- `juice_observations`：规范化数字、哈希、长度、状态码和匹配标签，不含原始回复。
- `prompt_variant_template_id`：本轮实际选择的算式与包装器编号；
- `prompt_variant_text`：实际发送的 Juice 用户提示正文；
- `request_body`：实际发送的 JSON 请求体，不含请求头、Authorization 或 API key；
- `configuration.juice_prompt_variant_mode`：当前使用的公开回退或内部私有模板版本；
- `configuration.juice_cycle_policy=always_high_optional_one_random_auxiliary`：每轮固定高档并随机追加零或一个辅助档；
- `configuration.juice_high_window=20`、`juice_other_window=10`：分档滚动窗口容量；
- `configuration.detection_mode`：`combined` 或 `juice_only`；
- `trusted_source_health_reported_separately=true`：表示 v3.1.1 已加载并单独报告可信来源健康度。

报告不保存 API key、原始挑战、正确答案、候选原始回答或原始密文。

每次写入 JSON 时还会原子更新同名 HTML 简明报告。HTML 仅包含汇总结论和脱敏统计，JSON 仍是完整机器报告。

# GPT-5.6 API 综合检测 v3.1.1：技术依据


## 1. 检测目标

v3.1.1 回答三个不同问题：

1. 待测端能否消费可信 GPT-5.6 产生的加密推理状态；
2. 正确答案是否确实来自该加密状态，而不是泄漏、缓存或猜测；
3. 待测端的可见内部参数指纹更像 Sol、Terra、Luna 还是较低型号，是否出现混用。

第一、二项组成强检测。第三项是浅层辅助指纹。两类证据不会混成一个模糊分数，而是分别报告后再组合。


## 2. 强挑战的构造

客户端用 `secrets` 生成新的十位数字 A，并在本地计算唯一答案 B。任务按进入候选分母的次数均衡轮换：

- 反转十位字符串；
- 左移三位；
- 逐位计算 `9-d`。

可信模型只收到 A 和任务，必须在隐藏计算中保留 B，可见正文严格输出 `READY`。请求要求 Responses API 返回 `reasoning.encrypted_content`，并设置 `store=false`。

可信响应必须具备非空密文、正确可见正文且没有明文泄漏。随后可信模型使用规范召回提示自证 B。自证失败的状态不会发送给待测端，因为它没有形成可验证的因变量。


## 3. 四种因果对照

可信状态自证成功后，待测端依次接收四种随机顺序的上下文：

| 条件 | 内容 | 预期 |
|---|---|---|
| 完整状态 | reasoning、密文、message、item ID | 精确返回 B |
| 删除 ID | 内容不变，删除 item ID | 精确返回 B |
| 只有正文 | 只保留 `READY` | 返回 `UNKNOWN` |
| 损坏密文 | 改变密文中间一个字符 | 返回 `UNKNOWN` |

候选请求不包含 A、B 或首轮任务提示。发送前还会序列化脱敏后的请求并检查 B 是否意外出现。

两项阳性证明可用性；两项阴性约束信息来源。阴性条件精确返回 B 会触发 `suspicious`，而不是计为成功。


## 4. 分母与网络稳定性

删除候选错误轮次会产生选择偏差：中转可以只在成功路由到兼容模型时返回结果，其余请求返回 503，最终仍凑够成功样本。

因此 v3.1.1 的最近 20 次窗口以“开始请求待测端的挑战”为分母，包括最终传输失败的挑战。429、500、502、503、504 和无状态码连接错误使用有限随机退避；403 等非瞬时拒绝不重试。

模型正常返回错误答案或 `UNKNOWN` 时绝不重试，避免通过反复抽样挑选正确回答。

重试与最终失败都如实保留并进入线路评价，但不再作为模型能力的一票否决项。失败请求仍在 20 次分母中且不能贡献命中，因此无法通过删除失败轮次美化成功率。


## 5. 第一层判定

最近 20 个候选尝试同时满足以下条件，就输出加密状态兼容：

- 完整状态精确命中至少 15 次；
- 去掉状态编号后的状态精确命中至少 15 次；
- 两种无状态请求精确命中为 0；
- 明文泄漏为 0。

网络质量另行输出：

- smooth：没有最终失败，也没有重试；
- intermittent：没有最终失败，但至少一轮重试后恢复；
- unstable：至少一轮最终仍有请求失败。

因此，16/20 和 15/20 命中、反例为 0，即使其中发生过重试，仍属于强检测通过；报告同时标注线路波动。若线路失败过多导致任一有效条件不足 15 次，则仍无法通过能力门槛。

十位答案盲猜单次命中概率至多约为 10^-10。多次精确恢复能强力排除偶然猜测，但不能排除把探针透明转发给兼容后端。

这一层测量加密状态兼容域。若 Sol、Terra、Luna 共享解密能力，仅靠这一层不能区分具体变体。

## 6. juice 浅层指纹

Juice 探针只请求待测端，不经过可信端。探针从 Valid Channels 读取 Juice 值 J，要求执行结果仍为 J 的恒等变形，例如 J × 2 ÷ 2 或 J + 7 - 7。因此随机化不会引入新的型号数字映射，现有分类表始终接收原始 Juice。

公开包提供三个公开恒等模板。内部包额外加载 internal-identity-v1：从八个恒等算式和八个包装器中独立随机选取，并加入一次性 nonce。随机化用于减少完全相同的静态字节特征，不声称能抵抗语义分类、流量关联或主动探针识别。

程序只读取最终可见回复，不请求隐藏推理。可见回复分为四类：

- 纯数字：规范化后参与指纹匹配；
- 明确拒答：记为无证据；
- 其他文字：记为无证据；
- 接口错误：记为无证据并单独报告。

普通 Juice 原始回复仍不写入报告。为审计模板选择与缓存行为，报告保存规范化数字、回答长度与 SHA-256、提示变体 SHA-256、模板模式、模板 ID、提示正文、实际 JSON 请求体、状态码和匹配标签；高档字面量输出对照为诊断替换内容额外保存最多 128 字符的短可见回复。请求头、Authorization 和 API key 不保存。

## 7. 指纹规则与权重

可信校准显示高档最有区分力：

| 高档结果 | 主指纹 |
|---|---|
| `40` 或 `40` 开头且带后续位数 | GPT-5.6 Sol |
| `32` | GPT-5.6 Terra |
| `48` | GPT-5.6 Luna |
| `96` | GPT-5.5/5.4 组 |
| `64` | GPT-5.4 mini |

低档和中档帮助确认 Sol，并区分 Terra/Luna 或旧型号。Sol 的低档基础值 `8` 与 Luna/mini 重合，中档基础值 `16` 与 Terra/Luna 重合；重合值不会触发混用。超高档 `128` 在 Sol/Luna 间重合，最高档 `960` 在 Sol/Terra 间重合，因此只作辅助。

单次检测对五档各采样 3 次。持续监控每轮固定采样高档，再从“不加测、低、中、超高、最高”中均匀随机选一项；因此每轮有 1–2 个 Juice 请求，辅助档位出现概率为 4/5。高档普通分类使用最近 20 条，其他四档分别使用最近 10 条。高档主指纹至少 3 次且得到其他档位支持才达到高置信。

两种运行方式共用上述分类器：综合模式同时执行加密状态挑战；仅 Juice 模式完全不构造可信客户端，只输出辅助指纹结论，并把加密状态结果标记为 `not_run_juice_only`。

具体可信样本见 `JUICE_CALIBRATION_CN.md`。

### 高档字面量输出完整性对照

该对照不读取 Juice，也不参与型号分类。请求在 high 档随机要求可见正文严格等于 Luna 的 `48` 或 Terra 的 `32`，两者等概率，用于观察响应输出链路是否改写这两个高档特征值。单次模式固定执行一次并等概率选值；持续模式每轮先用加密随机数独立作 50% 执行选择，命中后再等概率选值。

严格比较使用完整可见文本，只有 `answer == expected_text` 通过。成功返回其他数字、拒答或文字会形成 `output_rewrite_suspected`，并在整个当前会话内粘住；最近 20 条仍用于显示近期精确、非精确和错误计数。传输错误不触发改写警报。

这个对照只能发现“输出没有原样到达”。模型未遵循指令、上游系统提示、内容过滤和中转响应替换都可能造成相同现象，因此它是输出完整性告警，不是中转篡改的密码学证明。


## 8. 混用零容忍

普通型号分类使用分档滚动窗口，但混用检测查看整个会话的全部高档有效数字和已知跨档冲突。

控制台的普通计数只采用滚动窗口口径：高档显示 `当前条数/20`，其他档显示 `当前条数/10`。至少 3 个一致高档数字仍是分类门槛，但不会作为窗口进度的分母。全量历史只用于会话级混用与跨档冲突，不并入普通通过数。

只要高档曾出现两个互斥主指纹，例如 `40855` 与 `48`，立即输出 `mixed_or_inconsistent`。该标记在当前会话中永久保留，不会因为后续多数结果一致而消失。重新启动全新会话才会清空。

高档确定主型号后，只要任一档位出现已知且明确排斥主型号的数字，也会立即形成会话级混用标记。能同时匹配主型号和其他型号的重合值不会触发。拒答、未知数字与网络错误不会被误认为其他型号。

这种零容忍规则减少漏报混用，但单次异常可导致会话长期报警，属于有意选择的保守取舍。


## 9. 综合判定

综合通过需要：

1. 第一层加密状态挑战严格通过；
2. juice 达到高置信分类；
3. 整个会话没有混用标记；
4. juice 型号与用户请求的模型标签一致。

线路质量不会改变以上综合真假结论，而是作为并列信息附在结论后。

可能输出：

- 两层检测一致；
- 第一层兼容，但具体变体样本不足；
- 第一层兼容，但变体与申报不符；
- 检测到模型混用或路由不一致；
- 第一层与较低型号指纹冲突；
- 模型证据通过，同时独立提示线路流畅、偶有波动或不稳定；
- 阴性对照异常或检测泄漏。

第一层没有通过时，juice 即使很像某个型号也不能独立产生综合通过。仅 Juice 模式的最高正面结论为“辅助通过”，不等价于综合通过。


### 监控健康度覆盖

持续监控把三种时间概念分开：

1. 报告更新时间：证明进程仍在写文件；
2. 最后候选时间：证明待测端最近收到过强挑战；
3. 滚动窗口时间范围：说明当前模型结论实际来自哪一段历史样本。

若可信端连续失败至少 3 轮，或最后候选超过动态阈值，基础综合结果不会被删除，但外层状态改为 historical_pass_trusted_source_unavailable，中文显示“历史检测通过，但当前监控未刷新”。

动态阈值为 max(300 秒, 2 × 最大轮间等待 + 请求超时)。这避免长间隔监控被误判为失效，也能让默认快速监控在约 5 分钟内发现证据停止刷新。

可信端失败退避为 60、120、240 秒并封顶。新候选成功后连续失败计数清零，健康状态立即恢复；会话级混用记录继续保留。

这种覆盖只改变“当前是否仍在有效监控”的表述，不篡改历史强检测、juice 或待测线路统计。
## 10. 可信度边界

juice 是经验指纹，不是公开稳定协议。服务端升级、内部参数变化、系统提示、拒答策略都可能改变输出。

检测器源码公开后，恶意中转可以识别固定提示词并伪造预期数字，也可以只把探针流量路由给特定模型。随机挑战、顺序和时间只能提高识别成本，不能实现不可伪装。

因此：

- 加密状态层提供较强能力证据；
- juice 层提供中高置信的具体变体线索和混用报警；
- 两层一致比任何一层单独结果更可靠；
- 结果仍不能证明物理后端归属，也不能保证普通业务请求采用同一路由。


## 11. 数据最小化

JSON 报告不保存 API key、原始十位挑战、正确答案、候选原始回答或原始密文。

报告保存：

- URL 和用户填写的模型标签；
- 条件命中、状态码、耗时、重试；
- 答案长度与哈希；
- 密文长度与哈希；
- 规范化 juice 数字及其匹配标签；
- Juice 提示模板 ID、提示正文和实际 JSON 请求体，不含请求头或 Authorization；
- 第一层、第二层和综合结论。

key 只存在于检测子进程环境，进程退出后消失。

### 报告展示层

检测器将 JSON 作为唯一完整事实来源，并在每次写盘后生成同名静态 HTML。HTML 只读取报告中的汇总结论、窗口计数、健康度和脱敏异常，不运行脚本、不加载远程资源，也不保存 API key。Tkinter 图形界面同样只启动现有检测子进程并轮询 UTF-8 JSON，不复制或重新实现检测判定逻辑。

# GPT-5.6 API 综合检测 v3.1：技术依据


## 1. 检测目标

v3.1 回答三个不同问题：

1. 待测端能否消费可信 GPT-5.6 产生的加密推理状态；
2. 正确答案是否确实来自该加密状态，而不是泄漏、缓存或猜测；
3. 待测端的可见内部参数指纹更像 Sol、Terra、Luna 还是较低型号，是否出现混用。

第一、二项组成强检测。第三项是浅层辅助指纹。两类证据不会混成一个模糊分数，而是分别报告后再组合。


## 2. 强挑战的构造

客户端用 `secrets` 生成新的十位数字 A，并在本地计算唯一答案 B。任务按进入候选分母的次数均衡轮换：

- 反转十位字符串；
- 左移三位；
- 逐位计算 `9-d`。

可信模型只收到 A 和任务，必须在隐藏计算中保留 B，可见正文严格输出 `READY`。请求要求 Responses API 返回 `reasoning.encrypted_content`，并设置 `store=false`。

可信响应必须具备非空密文、正确可见正文且没有明文泄漏。随后可信模型使用规范召回提示自证 B。自证失败的状态不会发送给待测端，因为它没有形成可验证的因变量。


## 3. 四种因果对照

可信状态自证成功后，待测端依次接收四种随机顺序的上下文：

| 条件 | 内容 | 预期 |
|---|---|---|
| 完整状态 | reasoning、密文、message、item ID | 精确返回 B |
| 删除 ID | 内容不变，删除 item ID | 精确返回 B |
| 只有正文 | 只保留 `READY` | 返回 `UNKNOWN` |
| 损坏密文 | 改变密文中间一个字符 | 返回 `UNKNOWN` |

候选请求不包含 A、B 或首轮任务提示。发送前还会序列化脱敏后的请求并检查 B 是否意外出现。

两项阳性证明可用性；两项阴性约束信息来源。阴性条件精确返回 B 会触发 `suspicious`，而不是计为成功。


## 4. 分母与网络稳定性

删除候选错误轮次会产生选择偏差：中转可以只在成功路由到兼容模型时返回结果，其余请求返回 503，最终仍凑够成功样本。

因此 v3.1 的最近 20 次窗口以“开始请求待测端的挑战”为分母，包括最终传输失败的挑战。429、500、502、503、504 和无状态码连接错误使用有限随机退避；403 等非瞬时拒绝不重试。

模型正常返回错误答案或 `UNKNOWN` 时绝不重试，避免通过反复抽样挑选正确回答。

重试与最终失败都如实保留并进入线路评价，但不再作为模型能力的一票否决项。失败请求仍在 20 次分母中且不能贡献命中，因此无法通过删除失败轮次美化成功率。


## 5. 第一层判定

最近 20 个候选尝试同时满足以下条件，就输出加密状态兼容：

- 完整状态精确命中至少 15 次；
- 去掉状态编号后的状态精确命中至少 15 次；
- 两种无状态请求精确命中为 0；
- 明文泄漏为 0。

网络质量另行输出：

- smooth：没有最终失败，也没有重试；
- intermittent：没有最终失败，但至少一轮重试后恢复；
- unstable：至少一轮最终仍有请求失败。

因此，16/20 和 15/20 命中、反例为 0，即使其中发生过重试，仍属于强检测通过；报告同时标注线路波动。若线路失败过多导致任一有效条件不足 15 次，则仍无法通过能力门槛。

十位答案盲猜单次命中概率至多约为 10^-10。多次精确恢复能强力排除偶然猜测，但不能排除把探针透明转发给兼容后端。

这一层测量加密状态兼容域。若 Sol、Terra、Luna 共享解密能力，仅靠这一层不能区分具体变体。

## 6. juice 浅层指纹

v3.1 使用一个 XML 形状的用户提示，要求模型计算：

```text
Juice number / 2 * 10 / 5
```

这个算式等于原 Juice number。提示同时要求只输出结果。程序只读取最终可见回复，不请求隐藏推理。

可见回复分为四类：

- 纯数字：规范化后参与指纹匹配；
- 明确拒答：记为无证据；
- 其他文字：记为无证据；
- 接口错误：记为无证据并单独报告。

原始回复不写入报告，只保存规范化数字、长度、SHA-256、状态码和匹配标签。


## 7. 指纹规则与权重

可信校准显示高档最有区分力：

| 高档结果 | 主指纹 |
|---|---|
| `40` 或 `40` 开头且带后续位数 | GPT-5.6 Sol |
| `32` | GPT-5.6 Terra |
| `48` | GPT-5.6 Luna |
| `96` | GPT-5.5/5.4 组 |
| `64` | GPT-5.4 mini |

低档和中档帮助确认 Sol，并区分 Terra/Luna 或旧型号。Sol 的低档基础值 `8` 与 Luna/mini 重合，中档基础值 `16` 与 Terra/Luna 重合；重合值不会触发混用。超高档 `128` 在 Sol/Luna 间重合，最高档 `960` 在 Sol/Terra 间重合，因此只作辅助。

单次检测对五档各采样 3 次。持续监控按 `高、低、高、中、高、超高、最高` 循环，每档普通分类使用最近 3 次，高档主指纹至少 3 次且得到其他档位支持才达到高置信。

具体可信样本见 `JUICE_CALIBRATION_CN.md`。


## 8. 混用零容忍

普通型号分类使用最近样本，但混用检测查看整个会话的全部高档有效数字。

只要高档曾出现两个互斥主指纹，例如 `40855` 与 `48`，立即输出 `mixed_or_inconsistent`。该标记在当前会话中永久保留，不会因为后续多数结果一致而消失。重新启动全新会话才会清空。

高档确定主型号后，只要任一档位出现已知且明确排斥主型号的数字，也会立即形成会话级混用标记。能同时匹配主型号和其他型号的重合值不会触发。拒答、未知数字与网络错误不会被误认为其他型号。

这种零容忍规则减少漏报混用，但单次异常可导致会话长期报警，属于有意选择的保守取舍。


## 9. 综合判定

综合通过需要：

1. 第一层加密状态挑战严格通过；
2. juice 达到高置信分类；
3. 整个会话没有混用标记；
4. juice 型号与用户请求的模型标签一致。

线路质量不会改变以上综合真假结论，而是作为并列信息附在结论后。

可能输出：

- 两层检测一致；
- 第一层兼容，但具体变体样本不足；
- 第一层兼容，但变体与申报不符；
- 检测到模型混用或路由不一致；
- 第一层与较低型号指纹冲突；
- 模型证据通过，同时独立提示线路流畅、偶有波动或不稳定；
- 阴性对照异常或检测泄漏。

第一层没有通过时，juice 即使很像某个型号也不能独立产生综合通过。


## 10. 可信度边界

juice 是经验指纹，不是公开稳定协议。服务端升级、内部参数变化、系统提示、拒答策略都可能改变输出。

检测器源码公开后，恶意中转可以识别固定提示词并伪造预期数字，也可以只把探针流量路由给特定模型。随机挑战、顺序和时间只能提高识别成本，不能实现不可伪装。

因此：

- 加密状态层提供较强能力证据；
- juice 层提供中高置信的具体变体线索和混用报警；
- 两层一致比任何一层单独结果更可靠；
- 结果仍不能证明物理后端归属，也不能保证普通业务请求采用同一路由。


## 11. 数据最小化

JSON 报告不保存 API key、原始十位挑战、正确答案、候选原始回答或原始密文。

报告保存：

- URL 和用户填写的模型标签；
- 条件命中、状态码、耗时、重试；
- 答案长度与哈希；
- 密文长度与哈希；
- 规范化 juice 数字及其匹配标签；
- 第一层、第二层和综合结论。

key 只存在于检测子进程环境，进程退出后消失。